---
- name: install tomcat 10 on ubuntu 22
  hosts: all
  become: no
  tasks:
    - name: java installation
      ansible.builtin.apt:
        name: openjdk-11-jdk
        update_cache: yes
        state: present